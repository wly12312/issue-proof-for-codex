interface:
  display_name: "Codex Maintenance Receipt"
  short_description: "Windows issue reproduction and verification receipts"
  default_prompt: "Use $reproduce-github-issue to baseline an issue, inspect a Codex trace, and create a draft maintenance receipt."
