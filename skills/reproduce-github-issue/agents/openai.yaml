interface:
  display_name: "Issue Reproduction and Verification"
  short_description: "One-conversation Windows issue evidence workflow"
  default_prompt: "Use $reproduce-github-issue to read the Issue through the current Codex GitHub connector, create two command-argv baselines, edit and test only when authorized, independently verify, and create a local receipt without requiring a trace."
